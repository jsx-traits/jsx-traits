export function loader() {
  return Response.json({
    name: "JSX Traits",
    short_name: "JSX Traits",
    description: "Ordered, typed behavior composition for JSX elements.",
    display: "standalone",
    background_color: "#f4f1ea",
    theme_color: "#17151f",
  });
}
